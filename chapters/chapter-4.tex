\chapter{Results}\label{Ch:Results}
The presentation of the results will follow the order of the workflow: First of all the curated datasets will be displayed with the addition of statistical parameters that can be used to evaluate the difference between the samples in the datasets and the synthetic \gls{gan} samples. Secondly, the post training results are presented which presents the results of the hyperparameter tuning, the cGAN unconstrained performance to grouped facies classes as well as the cGAN unconstrained performance when presented with all facies classes. Finally, the results of the optimization and inversion results are presented, after the model was constrained to the Delft Geothermal Reservoir well data.   
\section{Datasets}\label{Sec:Datasets}
In total, 20,000 samples were generated for the three datasets as defined in table~\ref{tab:NEXUS_input}. These generated datasets are visually summarized in figure~\ref{fig:3D_datasets_comparison}: a 2D MDS plot clearly shows a grouping effect per setting, indicating the samples of different settings do in fact display varying higher-dimensional differences. This can be visually confirmed by the two random 3D facies plots per setting, showing that a lower sand body extension as input parameter yields samples with thinner sandbodies and more erratic meandering behavior in samples of this size. The right most plots show the pixel-wise entropy computed over an ensemble of 1000 samples per setting, setting a baseline for the entropy readings we look to achieve from a diverse synthetic dataset produced by the trained GAN model. This threshold configuration can be further evaluated by analyzing table~\ref{tab:comparison_facies_distribution_flumy_samples}, which provides an exhaustive overview of the global facies distributions across the ensemble.
\begin{table}[htbp!]
    \centering
    \renewcommand{\arraystretch}{1.4}
    \small
    \begin{tabularx}{\textwidth}{Xccc}
        \hline
        \textbf{Facies} & \textbf{Setting 1 (\%)} & \textbf{Setting 2 (\%)} & \textbf{Setting 3 (\%)}\\
        \hline
        \textbf{1: Channel Lag} & 1.6 & 1.8 & 1.8 \\
        \textbf{2: Point Bar} & 67.5 & 66.3 & 65.4 \\
        \textbf{3: Sand Plug} & 1.8 & 1.6 & 1.4 \\
        \textbf{4: Crevasse Splay Core} & 0.0 & 0.0 & 0.0 \\
        \textbf{5: Crevasse Channel} & 0.1  & 0.1 & 0.1 \\
        \textbf{6: Crevasse Splay Delta} & 0.0 & 0.0 & 0.0 \\
        \textbf{7: Levee} & 5.1 & 6.5 & 7.7 \\
        \textbf{8: Overbank} & 10.2 & 11.4 & 11.9 \\
        \textbf{9: Mud Plug} & 13.6 & 12.4 & 11.5 \\
        \hline
    \end{tabularx}
    \caption{Global categorical facies distribution profiles compared across the three synthetic training datasets for all available classes.}
    \label{tab:comparison_facies_distribution_flumy_samples}
\end{table}
As can be observed in this overview, the structural facies, hereafter referred to as classes, exhibit a severe statistical imbalance throughout the data volume. This severe imbalance is a result of dominant classes occupying over 60\% of the total grid volume, while corresponding minority classes are entirely absent due to the fundamental boundary limitations of the FLUMY NEXUS parameters. The volumetric presence of classes 4, 5, and 6 is particularly suppressed, yielding an exceptionally low occurrence rate ranging between 0.0\% and 0.1\%. Altough the classes will be grouped to form three classes instead of nine, the class imbalance will remain with the second class only occuring around 5|5 of the time while the first class occurs approximatly 70\% of the time. Such depressed occurrence rates could present an optimization bottleneck as the random occurrence of these volumes throughout the samples could confuse the model into finding an optimized setting that totally disregards these classes. The Discriminator could get stuck on deciding whether a sample is real or fake by judging mostly based on class 1, which leads to the Generator trying to improve the structure of this class, completely disregarding the other classes. Moreover, the high class imbalance in the data has to be accounted for when evaluating the model, especially with regards to the well mismatch evaluation. 
\begin{figure}[htbp!]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/results/datasets_summary.png}
    \caption{MDS plot with two 3D facies samples displayed and a 3D pixel-wise entropy per setting.}
    \label{fig:3D_datasets_comparison}
\end{figure}
% -------------------------------------------------
% ------- POST TRAINING GENERATIVE RESULTS --------
% -------------------------------------------------
\section{Post Training Generative Results}\label{Sec:Post_training_res}
All the models were trained on 2 NVIDIA-A100 GPU's with each 80GB of unified memory. When trained for 25 epochs on 20000 samples, the excecution time took approximately 3-4 hours.
\subsection{Hyperparameter Tuning}\label{Sec:Hyperparam_results}
Figure~\ref{fig:ablation_study} and table~\ref{tab:ablation_study} provide an overview of the hyperparameter tuning study. All trained model reserved 10\% of their training data exclusively for validation: computing the \gls{swd} metric for both training and validation datasets. Figure~\ref{fig:ablation_study} includes tracking of the Sliced Wasserstein Distance (SWD) metric across 25 epochs, a randomly generated sample by the trained model and a 3D entropy plot of an ensemble of 1000 \gls{gan} generated samples. Complementary, the Jenson-Shannon Divergence (JSD) and Mean SWD which both compare the ensemble of 1000 generated samples per run to a test set of 1000 samples is provided in table~\ref{tab:ablation_study}. 
\begin{table}[htbp!]
    \centering
    \renewcommand{\arraystretch}{1.4}
    \resizebox{\textwidth}{!}{%
    \begin{tabular}{cccccccc}
        \hline
        \textbf{Run} & \textbf{Epochs}& \textbf{disc penalty} & \textbf{double conv} & \textbf{double resblock} & \textbf{JSD $\downarrow$} & \textbf{MS-SWD $\downarrow$} & \textbf{mean $H_{norm}$ $\uparrow$}\\
        \hline
        1 & 25 & no & off & off & 0.0718 $\pm$ 0.0157 & 0.08865 & \\
        2 & 25 & no & on & off & 0.1161 $\pm$ 0.0243 & 0.06842 & \\
        3 & 25 & no & on & on & 0.0458 $\pm$ 0.0076 & 0.08271 & \\
        4 & 25 & 32 & on & on & 0.0612 $\pm$ 0.0314 & 0.09224 & \\
        \textbf{5} & \textbf{25} & \textbf{16} & \textbf{on} & \textbf{on} & \textbf{0.0155 $\pm$ 0.0067} & \textbf{0.07986} & \\
        6 & 25 & 1 & on & on & 0.0202 $\pm$ 0.0038 & 0.07690 & \\
        7 & 25 & 16 & on & on & 0.1190 $\pm$ 0.0324 & 0.11924 & \\
        8 & 25 & 16 & on & on & 0.0278 $\pm$ 0.0045 &  0.13389 & \\
        \hline
    \end{tabular}%
    }
    \caption{Overview of hyperparameters and evaluation metrics of each run. Run 5, based on architecture 4 by~\parencite{rongier2025atowards} yields the best JSD and second to best MS-SWD value.}
    \label{tab:ablation_study}
\end{table}
\begin{figure}[htbp!]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/results/Ablation study.png}
    \caption{Ablation study starting from \gls{fluvgan} and progressively adding/adjusting parameters to observe their effect on training, geological plausibility and sample diversity.~\textit{Run 1:} Standard FluvGAN architecture from~\parencite{rongier2025atowards}.~\textit{Run 2:} Addition of double convolutional layers.~\textit{Run 3:} Addition of double residual blocks (i.e. skip connections) on top of double convolutions.~\textit{Run 4:} Addition of a discriminator penalty every 32 iterations.~\textit{Run 5:} CHanged discriminator penalty to every 16 iterations.~\textit{Run 6:} Changed discriminator penalty to every iteration.~\textit{Run 7:} Run 5 hyperparameters but decreased to 10000 training samples.~\textit{Run 8:} Run 5 hyperparameters but changed number of itereation of the discriminator to 2.}
    \label{fig:ablation_study}
\end{figure}
Based on the metrics from table~\ref{tab:ablation_study} and entropy plots from figure~\ref{fig:ablation_study}, the best model was achieved in run 5 with a JSD of 0.0155, MS-SWD of 0.07986 and with the best pixel-wise entropy value out of all the runs (0.79), supported by the 3D entropy plot which shows almost no structure visible in any region. The MS-SWD metric showed all samples decreasing in Sliced Wasserstein's Distance, indicating a better fit to the training dataset. An interesting observation with regards to the \gls{swd} evolution over 25 epochs can be made: All plots show a stabilizing SWD after 2000 training iterations. From a \gls{swd} metric standpoint, this indicates that the model has stopped improving its samples compared to the validation dataset. 

Moreover, the base \gls{fluvgan} architecture lead to significant mode collapse looking at figure~\ref{fig:ablation_study}. Only after increasing the model depth (double convolutions \& double residual blocks) did the model train properly without mode collapse. Moreover, when varying the discriminator R1 Regularization, applying the penalty every 16 iterations yielded the best result. All other option (no penalty, every 32 iterations and every iteration) all resulted in mode collapse. Finally, run 7 and 8 investigated whether decreasing the number of training samples from 20000 to 10000 and only updating the discriminator every 2nd iteration, yielded any improved results. Where the results from run 8 yielded similar results to run 6, the metrics from run 8 yielded a much higher mean JSD of 0.1190 $\pm$ 0.0324. Additionally, looking at teh entropy plot of run 8 in figure~\ref{fig:ablation_study}, The plot shows a significantly higher percentage of regions with low entropy values, indicating that most samples produced by the generator after training have similar pixel-wise values.

\subsection{cGAN Unconstrained Performance}
The model with run 5 parameters, of which a complete list with all exact parameters can be found in (add table to the appendix), was run for 100 epochs as well to see whether the results could improve. Altough the the JSD and MS-SWD values did not vary significantly, the geological plausibility of the samples seems to increase significantly, suggested by figure~\ref{fig:mds_samples_run_5}: when visually, comparing the evolution from 25 to 100 epochs, especially the continuity of the clay deposits, the continuity of this facies seems to have increased (add comparison figure between these two samples! -> 20 3s samples from 25 epochs and 20 samples from 100 epochs.). This is further supported by the blob mean blob size calculation (see table~)
\begin{figure}[htbpt!]
    \centering
    \includegraphics[width=0.75\textwidth]{figures/results/mds_samples_overview_matrix.png}
    \caption{Overview of model trained with run 5 settings as described in table~\ref{tab:ablation_study} and figure~\ref{fig:ablation_study} for 100 epochs. The MDS plot shows a 2D representation of a high dimensional structure computed with the Wasserstein distance. For reference have 100 samples from run 6 been added, showing that those samples do not overlap with the training dataset samples, suggesting they lack in full complexity.}
    \label{fig:mds_samples_run_5}
\end{figure}
As already noted in section~\ref{sec:Post_training_res}, the \gls{swd} metric evolution stops improving after around 2000 training iterations, suggesting that the model has stopped improving. However, when assessing the samples from a visual perspective, ... ADD EVOLUTION... 
\pagebreak
\subsection{High-Fidelity 9-Class Generative Performance}
Besides only evaluating the model trained on the grouped facies, the training was also conducted on the high fidelity case: testing whether reverting to 9 facies classes lead to significantly different results in terms of geological plausibility and sample diversity. Since the classes are highly unbalanced, this run tests what the capability limits of this model is. The model was trained for 25 epochs on 20000 samples, with the same hyperparameters as run 5. The results are summarized in figure~\ref{fig:combined_plot_all_channels}. The \gls{swd} evolution indicates that the models ability to represent class sctuctures, varies significantly per class. For channels 0 to 2 and 6 to 8 the \gls{swd} metric decreases immediatly from the start of training, indicating that the model is able to represent these classes well. However, for channels 3 ad 5, the \gls{swd} metric no improvement is made, indicating that the model is not able to represent these classes well. Channel 4 decreases over the full amount of iterations with the possibility to decrease even more. Finally, channel 0, channel 2 and channel six, which had a low \gls{swd} from the start onwards, display occasional increases in the metric. 

\begin{figure}[htbp!]
    \centering
    \includegraphics[width=0.95\textwidth]{figures/results/combined_plot_all_channels.png}
    \caption{Overview of (best) high fidelity model trained over 25 epochs. }
    \label{fig:combined_plot_all_channels}
\end{figure}
From a visual perspective, the samples show signs of being able to recreate geological features. This claim is supported by the \gls{mds} plot (ADD FIgure to appendix!) and the corresponding 3D facies plots. However, when evaluating the pixel-wise entropy plot we can observe that the samples show a significant amount of structure, indicating that the model is not able to represent the full complexity of the training dataset. This is further supported by the mean pixel-wise entropy value of ??, which is significantly lower than the mean pixel-wise entropy value of 0.79 for the grouped facies case.
% ----------------------------------------------------
% ------- POST CONDITIONING/INVERSION RESULTS --------
% ----------------------------------------------------
\pagebreak
\section{Post-Inversion Results}\label{Sec:Post_Cond_Res}
During optimization and inversion, the tuned model was conditioned to the well data presented in Section \ref{Sec:Delft_Reservoir}. The evaluation metrics were computed exclusively on the first 32 meters of well data from DEL-GT-01 and DEL-GT-02-S2, yielding a total of 64 spatial conditioning points for the Macro F1-score evaluation. During the initial Latent Space Optimization phase, 100 latent vectors were optimized to fit the well data. These outputs are henceforth referred to as \say{realizations}, as they represent models explicitly conditioned to real-world subsurface observations.
\begin{figure}[htbp!]
    \centering
    \includegraphics[width=0.9\textwidth]{figures/results/cgan_constrained_performance/comparative_f1_scores.png}
    \caption{}
    \label{fig:post_optimization_f1_scores}
\end{figure}

As illustrated in Figure \ref{fig:post_optimization_f1_scores}, the percentage of realizations achieving a flawless fit with the conditioning points is highly dependent on the optimization threshold ($Th$). The threshold ($Th$) defines a specific spatial radius around the wellbore, measured in voxels, within which the generative model is penalized for mismatching the hard conditioning data. Specifically, voxels located beyond this distance threshold are assigned a weight of zero in the context loss function, ensuring that the inversion process enforces localized geological alignment without over-constraining the broader, unobserved reservoir architecture~(See figure~\ref{fig:anisotropic_mask_appendix}). Table~\ref{tab:optimization_f1_scores} details the Macro F1-scores and spatial entropy metrics achieved across these varying threshold configurations.

\begin{table}[htbp!]
    \centering
    \renewcommand{\arraystretch}{1.4}
    \small
    \newcolumntype{Y}{>{\centering\arraybackslash}X} 
    \begin{tabularx}{\textwidth}{X c c c c c c}
        \hline
        \textbf{Run} & \textbf{Th} & \textbf{Macro F1$\uparrow$} & \textbf{Mean $H_{norm, P}\uparrow$} & \textbf{Mean $H_{norm, I}\uparrow$} & \textbf{Count $P \uparrow$} & \textbf{Count $I \downarrow$} \\
        \hline
        Run 1 & 1  & 0.618539 & 0.641889 & 0.664907 & 36 & 64 \\
        Run 2 & 2  &          &          &          &    &    \\
        Run 3 & 5  & 0.835997 & 0.634802 & 0.629816 & 70 & 30 \\
        Run 4 & 10 & 0.861774 & 0.596361 & 0.582823 & 73 & 27 \\
        Run 5 & 15 &          &          &          &    &    \\
        \hline
    \end{tabularx}
    \caption{Optimization metrics evaluated across different run configurations. The column `Th` indicates the specific threshold value defining the spatial region affected by latent optimization. Subscript indices $P$ (Perfect) denote the subset of generated samples achieving a flawless well alignment ($\text{F1} = 1.0$), while $I$ (Imperfect) refers to the remaining sample subset that did not fit perfectly ($\text{F1} < 1.0$). Normalized spatial entropy values ($H_{norm}$) are provided respectively for both groups alongside categorical sample counts.}
    \label{tab:optimization_f1_scores}
    \label{tab:optimization_f1_scores}
\end{table}

Concurrently, the distribution of realizations achieving a perfect fit exhibits a rapid, non-linear response to the threshold parameter. Moving from a threshold of 1 (Run 1) to a threshold of 5 (Run 3) yields a near-exponential increase in the number of perfectly conditioned realizations, jumping from 36 to 70. However, as the threshold continues to increase past this point, the rate of improvement plateaus, reaching only 73 perfect realizations at a threshold of 10. This plateau suggests a fundamental limit to how much a frozen generator can bend its learned prior to fit localized data, demonstrating why subsequent Pivotal Tuning is required to bridge the remaining spatial mismatch.

Furthermore, Table \ref{tab:optimization_f1_scores} highlights a critical trade-off between strict well conditioning and spatial diversity. As the optimization threshold increases, the normalized spatial entropy ($H_{norm}$) systematically decreases for both the perfectly aligned ($P$) and imperfectly aligned ($I$) subsets. This mathematical drop in entropy indicates that enforcing stricter conditioning constraints forces the optimization algorithm to sample from a narrower, more restricted region of the latent space. Consequently, while the well data is honored more frequently, the overall stochastic diversity of the generated ensemble diminishes. Visual confirmation of this phenomenon is provided in Appendix \ref{App:Entropy_Thresholds}, which demonstrates that the spatial extent of low-entropy, high-certainty regions expands proportionally with the applied threshold.